import React from "react";
import { Card } from "react-bootstrap";

const Weather = () => {
  // return <div className="weather card"></div>;
  return <Card className="weather card">Today's Weather is:</Card>;
};

export default Weather;
